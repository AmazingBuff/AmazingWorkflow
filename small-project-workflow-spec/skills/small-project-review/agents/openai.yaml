interface:
  display_name: "Small Project Review"
  short_description: "Review iteration changes and package actionable fixes."
  default_prompt: "Use $small-project-review to review the current iteration or a focused repair delta."
policy:
  allow_implicit_invocation: false
